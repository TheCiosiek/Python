import random
def digitize(number):
    str(number)
    tab=[]
    for i in number:
        tab.append(i)
    print (tab)

number=input("Wprowadź liczbę, którą chcesz wprowadzić: ")
digitize(number)

